#include <bits/stdc++.h>
using namespace std;

int main(){
    // global variables
    string bottomK_path = "../bottomK_bins/arxiv_bottomK_1024.bin";
    const int max_k = 1024;
    int K = 64;
    
    // OverlapJoin Parameters
    int c = K - 4;

    // Input bottom_k and shrink their size to the specified K


    // Sort bottom_k based on their length, elements

    // Use overlapJoin

    // Investigate the result
    
}